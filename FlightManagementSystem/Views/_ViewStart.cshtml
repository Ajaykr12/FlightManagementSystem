@* @using FlightManagementSystem.Models
@using FlightManagementSystem.DTO
@{
    string layout = "";
    if (UserDTO.Role != null)
    {
        if (UserDTO.Role == "Admin" && Check.IsLoggedIn)
        {
            layout = "~/Views/Shared/AdminLayout.cshtml";
        }
        if (UserDTO.Role == "User" && Check.IsLoggedIn)
        {
            layout = "~/Views/Shared/PassengerLayout.cshtml";
        }
    }

    else
    {
        layout = "~/Views/Shared/_Layout.cshtml";
    }
    Layout = layout;
}  *@

@{
    Layout = "_Layout";
}